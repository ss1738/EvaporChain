## Addendum --- audit-2026-05-15 WIP-branch merge sweep (session 40)

Merged 7 completed audit branches that had accumulated unmerged on Mini 1.

| Branch | Finding | Status |
|---|---|---|
| audit/sh-compact-1-pair-dedup | SH-COMPACT-1 dead-pair dedup in find_candidates | **CLOSED** merged 9970a3e4 |
| audit/sh-compact-2-proof-hash-dst | SH-COMPACT-2 DST for ShardCompactionProof::compute_hash | **CLOSED** merged d348f815 |
| audit/sh-cross-1-bounded-pending | SH-CROSS-1 MAX_PENDING_RECEIPTS=64K + MAX_PER_SHARD_QUEUE=16K | **CLOSED** merged d1f2bbe9 |
| audit/sh-cross-2-receipt-dst | SH-CROSS-2 RECEIPT_LEAF_DST + RECEIPT_INTERNAL_DST | **CLOSED** merged b271bcad |
| audit/pay-rate-limiter-bounded-buckets | PAY-RATE-LIMITER-1 bound RateLimiter bucket map | **CLOSED** merged 3ef8090d |
| audit/scr-n1-cross-contract-caller-identity | SCR-N1 cross-contract caller identity (EOA leaked to callee) | **CLOSED** merged; fixed API drift in test |
| audit/priv-n2-restore-nullifiers-and-pnt | PRIV-N2 restore nullifiers + PNT window on startup | **CLOSED** merged 85497d12 |

550/550 execution, 70/70 sharding, 56/56 paymaster, 134/134 script pass. Push: 2de3b9c4.

---

## Addendum — audit-2026-05-15 Sharding + fee-controller sweep

Sweep: cross-shard message execution, receiver balance arithmetic, message payload size caps, message-ID replay, ordering semantics, fee controller PID arithmetic, Lyapunov function, parameter injection.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| CROSS-SHARD-001 | HIGH | `crates/evaporchain-execution/src/lib.rs:3051` (`execute_cross_shard_messages`) | Arithmetic overflow — `to_acct.balance += *amount` bare `+=` on u64; in release mode wraps silently if receiver near u64::MAX, reducing victim account to ~0 | **CLOSED** (`2c929a32` — `saturating_add`; adversarial canary `test_cross_shard_transfer_receiver_balance_saturates` added; 4/4 cross-shard tests pass) |
| CROSS-SHARD-002 | LOW | `crates/evaporchain-sharding/src/cross_shard.rs:27-28` (`MessagePayload::Query{key}`, `Eviction{reason}`) | Unbounded `String` fields — no size cap on deserialization; however all current production call sites use hardcoded bounded literals; no external API endpoint accepts `CrossShardMessage` from untrusted peers | **ACKNOWLEDGED** — low risk until a network-facing deserialization path is wired; add `MAX_QUERY_KEY = 1024` cap before any p2p cross-shard messaging is enabled |
| CROSS-SHARD-003 | DESIGN | `crates/evaporchain-sharding/src/cross_shard.rs:59` (`CrossShardRouter::next_id`) | In-memory-only message-ID counter — resets to 0 on router restart; ID reuse possible across epochs; experimental crate, no persistent message store yet | **ACKNOWLEDGED** — known limitation of in-memory-only router; persistent nonce store needed before production use |
| CROSS-SHARD-004 | FALSE POSITIVE | `crates/evaporchain-sharding/src/cross_shard.rs:86` (`drain_for_shard`) | Energy-based message ordering — doc comment at line 77-78 explicitly states "high-energy targets first"; intentional design, not a bug | **NOT A FINDING** — by design |
| FEE-ARITH-001 | CLEAN | `crates/evaporchain-fee-controller/src/controller.rs` | PID arithmetic all in i128; product bounded by ~10^31 << i128::MAX | **CLEAN** |
| FEE-PARAM-001 | CLEAN | `crates/evaporchain-execution/src/lib.rs` | Y-FEE-001 (`02c0bd6d`) present — bounds cap + cross-validation reset confirmed | **CLEAN** |
| LYAP-001 | CLEAN | `crates/evaporchain-fee-controller/src/lyapunov.rs:12` | `saturating_mul` for squaring; u128 conversion prevents overflow | **CLEAN** |
| FEE-DIVZERO-001 | CLEAN | `crates/evaporchain-fee-controller/src/controller.rs` | Divide-by-zero paths: divisors are `1_000_000` (constant) and `objects_for_avg` (guarded by `if > 0` check) | **CLEAN** |
| FEE-NEGFEE-001 | CLEAN | `crates/evaporchain-fee-controller/src/controller.rs:106` | `if total < 0` guard: unreachable because u64→i128 cast + saturating semantics keep total ≥ 0; defensive but harmless | **CLEAN** |

Commit `2c929a32`.

---

## Addendum — audit-2026-05-15 CLI keygen + network sybil scoring sweep

Sweep: CLI BLS keygen passphrase handling, Argon2id KDF key lifecycle, sybil-score accounting, peer-ban atomicity, disconnect/reconnect state, score decay, CHSH gate, proof-of-stake weighting.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| CLI-PASS-001 | MEDIUM | `crates/evaporchain-cli/src/main.rs` `cmd_encrypt_bls_key` / `cmd_decrypt_bls_key` | Passphrase lifetime — `resolve_passphrase()` returned `Vec<u8>`; without `Zeroizing<_>` wrapper the raw bytes were not overwritten before dealloc on all exit paths (including early error returns) | **CLOSED** (`e7a25629` — `Zeroizing::new(resolve_passphrase(...)?)` in both functions; drop-on-any-exit via `Zeroizing<Vec<u8>>`) |
| CLI-KDF-001 | MEDIUM | `crates/evaporchain-crypto/src/bls_key_store.rs` `encrypt_bls_secret_with_aad` / `decrypt_bls_secret_with_aad` (lines 152, 198) | KDF key lifetime — Argon2id-derived `[u8; 32]` key not explicitly cleared after `XChaCha20Poly1305::new_from_slice`; key material remained in stack/heap until OS reclaim | **CLOSED** (`e7a25629` — `Zeroizing::new(kdf(...)?)` wraps the derived key; `Drop` impl overwrites bytes on any exit including cipher-init error) |
| NET-SYBIL-001 | CLEAN | `crates/evaporchain-network/src/service.rs:sybil_score()` | Score accumulation — disconnected peers skip idle-tick penalties; `record_disconnect` clears score, `record_connect` fresh-slates; no idle-punishment accumulation against offline peers | **CLEAN** |
| NET-SYBIL-002 | CLEAN | `crates/evaporchain-network/src/service.rs:evict_bottom_percent()` | Eviction fairness — eviction samples bottom 5% by score, not by address prefix; no address-space griefing path | **CLEAN** |
| NET-SYBIL-003 | CLEAN | `crates/evaporchain-network/src/service.rs:peer_score_decay()` | Score decay — exponential decay applied per-peer-per-epoch; large positive score does not permanently whitelist a colluding peer cluster | **CLEAN** |
| NET-SYBIL-004 | CLEAN | `crates/evaporchain-network/src/service.rs:ban_peer()` + `BanList::save()` | Ban atomicity — temp+fsync+rename pattern already enforced (A1-close, `7f0ea5ff`); ban list durably persisted | **CLEAN** |
| NET-SYBIL-005 | CLEAN | `crates/evaporchain-network/src/service.rs:chsh_gate()` | CHSH weighting — Bell-CHSH gate score additive bonus only; cannot override quorum or fork-choice | **CLEAN** |
| NET-SYBIL-006 | CLEAN | `crates/evaporchain-network/src/service.rs:stake_weighted_score()` | Stake weighting — validator stake weight applied at consensus layer, not sybil layer; sybil score is a network-admission filter only | **CLEAN** |
| NET-SYBIL-007 | CLEAN | `crates/evaporchain-network/src/service.rs:max_peers` | Peer-count ceiling — `max_peers` enforced in `on_connection_established`; excess connections rejected before sybil scoring | **CLEAN** |

83/83 CLI tests pass; 69/69 crypto tests pass. Commit `e7a25629`.

---

## EvaporChain code audit — 2026-05-11

Focused, code-only audit. No documentation drift. Each finding cites
`file:line`, describes the exploit class, and proposes a minimal fix.

Closed items from earlier rounds (Audit-N, AUDIT_2026_05_06.md) were
spot-rechecked rather than re-listed; only **new** findings appear in
the table below.

### Findings

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| AUDIT-2026-05-11-1 | HIGH | `crates/evaporchain-network/src/service.rs:1761` | DoS — handler missing per-peer rate limit | **CLOSED** (rate_limiter.check_and_increment at :1761) |
| AUDIT-2026-05-11-2 | HIGH | `crates/evaporchain-network/src/service.rs:1769` | DoS — unbounded `queries.len()` drives Merkle-proof CPU | **CLOSED** (MAX_SHARD_QUERIES_PER_REQUEST + outbound mirror in `0a62a1d2`) |
| AUDIT-2026-05-14-A1 | CRITICAL | `crates/evaporchain-network/src/service.rs:save()` | Non-atomic write — crash mid-write left half-written JSON, re-admitting all banned peers on restart | **CLOSED** (`7f0ea5ff` — temp+fsync+rename, matching BanList::save() L7 pattern) |
| AUDIT-2026-05-14-A2 | CRITICAL | `crates/evaporchain-network/src/service.rs:save()` | Integer overflow — `now_wall + millis_left` wraps on large expiries, turning far-future bans into past times | **CLOSED** (`7f0ea5ff` — saturating_add) |
| AUDIT-2026-05-14-A3 | HIGH | `crates/evaporchain-network/src/service.rs:new_with_path()` | Unbounded Duration — `until_ms = u64::MAX` in corrupted file produced enormous Instant offset | **CLOSED** (`7f0ea5ff` — clamped to 2×BAN_DURATION before Duration::from_millis) |
| AUDIT-2026-05-14-B1 | MEDIUM | `crates/evaporchain-cli/src/bin/evaporchain-faucet.rs:save()` | Missing fsync — temp+rename without fsync; post-rename crash clears claim-store, allowing every IP to claim again | **CLOSED** (`b470c801` — write_all+sync_all+rename) |
| AUDIT-2026-05-14-B2 | MEDIUM | `crates/evaporchain-consensus/src/api.rs:persist_pools()` | Missing fsync — same class; Singh Pool ledger could silently reset to empty on crash, requiring operator recreation | **CLOSED** (`b470c801` — write_all+sync_all+rename) |
| AUDIT-2026-05-11-3 | MEDIUM | `crates/evaporchain-execution/src/privacy_exec.rs:906-915` | Arithmetic safety — raw `*`/`+` on u64 in fee-charging path | **CLOSED** (saturating_add + saturating_mul at :906-915) |
| AUDIT-2026-05-14-C1 | HIGH | `crates/evaporchain-consensus/src/tendermint.rs:stake_quorum_threshold()` | BFT quorum overflow — `total * 2` wraps if `total > u64::MAX/2`, making threshold tiny and trivially satisfiable | **CLOSED** (`5fc4debf` — u128 arithmetic, matching light-client path) |
| AUDIT-2026-05-14-C2 | HIGH | `evaporchain-da/src/{certificate,poha}.rs`, `consensus/src/{bridge,finality}.rs` | Supermajority overflow — `x * 3 >= y * 2` in u64 wraps when stake > u64::MAX/3, falsifying DA/bridge/finality quorum checks | **CLOSED** (`fe8b7291` — `(x as u128) * 3 >= (y as u128) * 2` across all 5 sites) |
| AUDIT-2026-05-14-NC1 | HIGH | `crates/evaporchain-consensus/src/tendermint.rs:submit_reveal()` | Regression — L10 `PENDING_REVEALS_CAP` guard deleted by commit `300440db` (Audit L9); `submit_reveal` back to unconditional `push`, reveal-spam re-enabled | **CLOSED** (`4234bc69` — cap + drop-at-cap guard restored; regression canary test added) |
| AUDIT-2026-05-14-D1 | LOW | `crates/evaporchain-execution/src/rewards.rs:423` | Defensive — `remainder -= 1` on u64 in dust-loop; safe per adjacent break guard but inconsistent with saturating-arithmetic convention | **CLOSED** (`abe1b3b4` — `saturating_sub(1)`; 538 execution tests pass) |
| AUDIT-2026-05-14-E1 | CRITICAL | `crates/evaporchain-node/src/api.rs:post_governance_param`, `post_governance_fork_choice_mode` | Access control — governance flag mutation endpoints had no auth; any peer could flip `conservation_enforcement`, `block_source_mode`, `crooks_mev_settlement_mode` etc. mid-consensus | **CLOSED** (`284f3fe4` — `require_admin_auth` gate) |
| AUDIT-2026-05-14-E2 | HIGH | `crates/evaporchain-node/src/api.rs:post_cartel_alarm_run_gate` | DoS — Bell CHSH gate computation is expensive; no auth allowed unbounded CPU flooding | **CLOSED** (`284f3fe4` — `require_admin_auth` gate) |
| AUDIT-2026-05-14-E3 | CRITICAL | `crates/evaporchain-node/src/api.rs:post_hbct_{mint,transfer,burn,settle}` | Access control — HBCT capacity token minting/transfer/settlement had no auth; unlimited minting possible | **CLOSED** (`284f3fe4` — `require_admin_auth` gate) |
| AUDIT-2026-05-14-E4 | CRITICAL | `crates/evaporchain-node/src/api.rs:post_sentinel_{register,vote,tick}` | Access control — governance parameter registration and voting had no validator identity check; governance takeover without quorum was possible | **CLOSED** (`284f3fe4` — `require_admin_auth` gate) |
| AUDIT-2026-05-14-E5 | MEDIUM | `crates/evaporchain-node/src/api.rs:post_{hbct_seed_demo,hbct_tick,sentinel_seed_demo,sentinel_seed_votes}` | Access control — 4 state-mutating seed/tick endpoints had no auth; allowed unauthorized state pollution | **CLOSED** (`7ef31cee` — `require_admin_auth` gate) |
| AUDIT-2026-05-14-E6 | MEDIUM | `crates/evaporchain-node/src/api.rs:post_oracle_ingest` | Timing side-channel — length-mismatch short-circuit on oracle API key leaked key length in O(1) requests | **CLOSED** (`7ef31cee` — `subtle::ConstantTimeEq` replaces XOR loop) |
| AUDIT-2026-05-14-F1a | HIGH | `crates/evaporchain-node/src/api.rs` (27 sites) | Panic via mutex poison — 27 raw `.lock().unwrap()` calls in HTTP handlers; a panic in any lock-holding task cascades to crash all subsequent calls on those endpoints | **CLOSED** (`c809ae41` — poison-tolerant `.unwrap_or_else` recovery across all 27 sites) |
| AUDIT-2026-05-14-F1b | HIGH | `crates/evaporchain-node/src/api.rs:post_swap_execute` | Panic — `.find().unwrap()` after `.any()` check; future edit or data inconsistency crashes the handler | **CLOSED** (`c809ae41` — `match` with JSON error response) |
| AUDIT-2026-05-14-F1c | HIGH | `crates/evaporchain-node/src/api.rs:get_da_{sample,light_sample}` | Panic — `BlockDA::new().unwrap()` crashes node on DA encoder init failure | **CLOSED** (`c809ae41` — `match` with `INTERNAL_SERVER_ERROR` response) |
| AUDIT-2026-05-14-G2 | MEDIUM | `crates/evaporchain-execution/src/lib.rs:1514,1594` | Allocation DoS — `serde_json::from_str(&tx.args)` with no length pre-flight; crafted multi-MB args JSON forces parse-tree allocation in execution layer before gas metering | **CLOSED** (`59b8a2de` — `tx.args.len() > 1_000_000` early-return guard in both `execute_call_contract` and `execute_call_script`) |
| AUDIT-2026-05-14-G3 | LOW | `crates/evaporchain-node/src/api.rs:create_router` | Implicit body limit — Axum's 2 MB cap is enforced only by the implicit framework default; a framework version bump could silently raise or remove it | **CLOSED** (`59b8a2de` — explicit `.layer(DefaultBodyLimit::max(2 * 1024 * 1024))` added to `create_router`) |
| AUDIT-2026-05-14-H1a | HIGH | `crates/evaporchain-node/src/api.rs:post_call_contract`, `post_call_script` | Input validation — `method` string has no per-byte gas metering; a 2MB method name would be hashed and HashMap-looked-up before any gas check fires | **CLOSED** (`6d03ec95` — `req.method.len() > 256` early-return in both call handlers) |
| AUDIT-2026-05-14-H1b | HIGH | `crates/evaporchain-node/src/api.rs:post_deploy_script` | Input validation — admission pre-check uses only flat `GAS_DEPLOY_SCRIPT`; a deployer with minimal balance could pass the balance gate and submit 2MB source to the EvaporScript parser | **CLOSED** (`6d03ec95` — `req.source_code.len() > 65_536` early-return in `post_deploy_script`) |
| AUDIT-2026-05-14-I1 | CRITICAL | `crates/evaporchain-execution/src/lib.rs:verify_tx_signature`, `crates/evaporchain-consensus/src/mempool.rs:validate_submission` | Signature–address binding missing — `HybridVerifier::verify` confirmed the signature was valid under the provided pk, but never checked `blake3(pk) == tx.from/caller/deployer/validator_address`; attacker could forge any tx as any victim address | **CLOSED** (`2dcf98ad` — `blake3(pk) == tx.sender()` binding in both execution and mempool layers; regression test `audit_i1_forged_sender_rejected` added) |
| AUDIT-2026-05-14-K4 | HIGH | `crates/evaporchain-consensus/src/tendermint.rs:bls_vote_message`, `src/bridge.rs:430` | Cross-chain replay — BLS vote messages lacked chain_id field; validator keys reused across testnets/mainnet could have precommit votes replayed in a different chain's consensus | **CLOSED** (`5a008085` — chain_id length-prefixed as first field; all 8 call sites updated including bridge.rs; bridge test helpers updated to match) |
| AUDIT-2026-05-15-M1 | MEDIUM | `crates/evaporchain-consensus/src/tendermint.rs:851` | Unbounded HashMap — `cross_fork_equivocations: HashMap<u64, u64>` accumulated stale entries for slashed/rotated validators indefinitely; no pruning on validator exit | **CLOSED** (`589ad2dd` — epoch-boundary `retain` keeps only IDs present in active ValidatorSet) |
| AUDIT-2026-05-15-N4 | CRITICAL | `crates/evaporchain-consensus/src/tendermint.rs:verify_da_certificate` | DA cert block binding missing — `verify_da_certificate` checked BLS sigs and supermajority but never checked `cert.block_number` against the block being verified; valid cert from block X could be reused to falsely certify block X+N, or an expired cert reused indefinitely | **CLOSED** (`2a3997a8` — reject `cert.block_number > block.number` + reject `cert.block_number < block.number - DA_CERT_MAX_LAG(12)`) |
| AUDIT-2026-05-15-N6 | HIGH | `crates/evaporchain-network/src/service.rs:2039` | mDNS routing-table pollution — mDNS discovery added peers to gossipsub routing table before `ConnectionEstablished` auth check fired; LAN attacker could pollute routing table with unauthorized peers (low exposure: mDNS off by default) | **CLOSED** (`0317a855` — `peer_authority.is_authorized` gate before `add_explicit_peer`) |
| AUDIT-2026-05-15-O1 | LOW | `crates/evaporchain-node/src/api.rs:get_oracle_feed` | Staleness opacity — `GET /api/oracle/feed/{key}` returned value+twap with no timestamp; operator and consumers had no signal that a feed had stopped updating | **CLOSED** (`baeed499` — response now includes `last_updated` (epoch secs) and `age_secs`; `oracle_bridge.get_last_updated()` helper added) |
| AUDIT-2026-05-15-O3 | INFO | `crates/evaporchain-node/src/oracle_bridge.rs:FreshnessConfig` | Single-source feeds — `FreshnessConfig::default()` sets `min_sources: 1`; production must use `FreshnessConfig::price_feed()` which sets `min_sources: 2` | **ACCEPTED** (configuration risk; code-level protection present in `price_feed()` variant; operator runbook responsibility) |
| AUDIT-2026-05-15-O5 | INFO | `crates/evaporchain-consensus/src/tendermint.rs:oracle_state_root` | Oracle root unverified — `oracle_state_root` field in block header is set per-validator but never consensus-verified; future oracle-reading contracts could diverge across nodes | **ACKNOWLEDGED** (design-level; oracle contracts not yet reading this field; tracked for post-mainnet cross-validator oracle trie) |
| AUDIT-2026-05-15-P1..P7 | — | `privacy_exec.rs`, `api.rs`, `evaporchain-script/src/vm.rs` | P-class sweep (nullifier double-spend, output commitment integrity, shield/unshield amount binding, ZK proof integration, UserOp privilege, governance bounds, EvaporScript privilege) | **CLEAN** — all 7 checks confirmed secure. Nullifier: layered (execution + PNT shadow). Amount binding: cryptographically enforced. UserOp: serial-only + paymaster sig gate + inner-tx allowlist. Governance: allowlist + numeric bounds. EvaporScript: lifecycle hook reentrancy guard active. Energy decay proofs: epoch-bounded (Stage 1; full ZK circuit verification deferred to Stage 2). |
| AUDIT-2026-05-15-Q3 | CRITICAL | `crates/evaporchain-consensus/src/tendermint.rs:4993` | Block timestamp future-bound missing — only monotonicity (no-decrease) was checked; proposer could set `block.timestamp = year_2050`, accepted by all validators, inflating energy-decay epoch calculations and manipulating tx ordering | **CLOSED** (`fde3a871` — `MAX_FUTURE_SECS=30` guard after monotonicity check; 3 adversarial tests: far-future rejected, +30s accepted, +31s rejected) |
| AUDIT-2026-05-15-Q1,2,4..7 | — | `tendermint.rs`, `mempool.rs`, `rewards.rs` | Q-class sweep (proposer selection, epoch boundary race, empty block injection, validator set update race, reward overflow, NIL vote liveness) | **CLEAN** — Q1: VRF verified when present, hash-based selection deterministic. Q2: validator set updates atomic at epoch boundary. Q4: downtime slashing + antichain mode. Q5: equivocation slash fires before finality accounting. Q6: saturating arithmetic throughout rewards. Q7: timeout-driven round advancement caps nil-vote liveness risk. |
| AUDIT-2026-05-15-R5 | CRITICAL | `crates/evaporchain-node/src/api.rs:3896,3922` | Missing auth on sentinel mutation endpoints — `POST /api/sentinel/register_param` and `POST /api/sentinel/vote` lacked `require_admin_auth`; unauthenticated caller could inject arbitrary governance parameters or impersonate any validator to cast sentinel votes | **CLOSED** (`d8e81d29` — `HeaderMap` extractor + `require_admin_auth` gate on both handlers; 241 node tests pass) |
| AUDIT-2026-05-15-R1,2,3,4,6,7 | — | `api.rs`, `service.rs`, `ws.rs` | R-class sweep (unbounded deserialization, JSON overflow, recursive JSON, path injection, CORS, WebSocket DOS) | **CLEAN** — R1: 2MB HTTP + 4MB gossip pre-checks before serde. R2: standard serde overflow handling. R3: typed struct deserialization only (no freeform Value). R4: validated path params, no shell/file/SQL injection. R6: explicit CORS allow-list, wildcard `*` actively refused. R7: 4096 subscriber cap. |
| AUDIT-2026-05-15-S1..S7 | — | `evaporchain-state/src/`, `node/src/main.rs`, `evaporchain-execution/src/` | S-class sweep (storage key collision, snapshot poisoning, WAL replay ordering, ghost growth, state root timing, concurrent access, write batch atomicity) | **CLEAN** — S1: separate RocksDB column families + content-addressed trie keys. S2: quorum-cert + body-hash + state-root dual verification before snapshot apply. S3: RocksDB native WriteBatch WAL provides crash atomicity (evaporchain custom WAL module built but unwired; RocksDB is sufficient). S4: `prune_before_height(epoch-1000)` wired at main.rs:5090,6365. S5: `save_full_block` before `commit_batch` (C3 ordering). S6: Block-STM MVCC + OCC conflict detection. S7: all multi-key mutations inside single WriteBatch. |

---

### AUDIT-2026-05-11-1 + 2 — ShardSample inbound is unrate-limited and unbounded

**Status: CLOSED 2026-05-12.** Inbound-request gate (rate limit + `MAX_SHARD_QUERIES_PER_REQUEST = 256` cap + ban-list violation) landed in `service.rs:1761-1779`. Outbound-response mirror (`validate_shard_sample_response_structure` + ban-list on rejection) landed in `0a62a1d2`. 2 unit tests pin the validator (empty + at-cap-vs-over-cap).

**Site:** `crates/evaporchain-network/src/service.rs:1739..1773`

**Observation:** the inbound `ShardSample` request handler

1. does **not** call `rate_limiter.check_and_increment(&peer)` (compare
   gossipsub at `:1548` and `BlockSync` at `:1614`, which both do), and
2. trusts `request.queries.len()` without an upper bound:
   `let mut samples = Vec::with_capacity(request.queries.len());`
   and then walks every query through `DASampler::generate_proof`
   (Merkle proof construction).

`ShardSampleRequest` (`:101-104`) is just `Vec<SampleQuery>` with no
length cap, and the `request_response::json::Behaviour` is built with
`Config::default().with_request_timeout(Duration::from_secs(10))` —
no `max_request_size`. The libp2p default cap for the JSON codec is
~1 MB; at ~24 bytes per `SampleQuery` that is ≈30 000 queries per
request. Each query runs a Merkle proof.

**Exploit:** a single peer can flood ShardSample with no rate
ceiling, each request forcing ~30k Merkle proof computations, while
its gossipsub and BlockSync rate-limits remain untouched (different
protocol → no shared counter). The other handlers are protected, this
one is not.

**Fix (≈10 lines):**

```rust
const MAX_SHARD_QUERIES_PER_REQUEST: usize = 256;

SwarmEvent::Behaviour(EvaporBehaviourEvent::ShardSample(
    request_response::Event::Message {
        peer,
        message: request_response::Message::Request { request, channel, .. },
    },
)) => {
    if !rate_limiter.check_and_increment(&peer) {
        warn!("Rate-limited peer {peer} — dropping shard-sample request");
        continue;
    }
    if request.queries.len() > MAX_SHARD_QUERIES_PER_REQUEST {
        warn!(
            "Peer {peer} sent {} shard queries, cap is {} — recording violation",
            request.queries.len(), MAX_SHARD_QUERIES_PER_REQUEST
        );
        ban_list.record_violation(peer);
        continue;
    }
    // ... existing handler ...
}
```

Mirror this gate on the outbound response path symmetrically with
`validate_shard_sample_response_structure`, matching the
`validate_sync_response_structure` pattern at `:138-163`.

---

### AUDIT-2026-05-11-3 — Private-tx gas estimator uses raw arithmetic

**Status: CLOSED.** Estimator at `privacy_exec.rs:906-915` already uses `saturating_add` + `saturating_mul` (matches the parallel.rs pattern the audit cites). Re-verified 2026-05-12.

**Site:** `crates/evaporchain-execution/src/privacy_exec.rs:877-882`

```rust
pub fn estimate_private_transfer_gas(tx: &PrivateTransferTx) -> u64 {
    GAS_PRIVATE_TRANSFER_BASE
        + GAS_PRIVATE_TRANSFER_PER_INPUT * tx.input_nullifiers.len() as u64
        + GAS_PRIVATE_TRANSFER_PER_OUTPUT * tx.output_commitments.len() as u64
}
```

Plain `*` and `+` on `u64`. With `GAS_PRIVATE_TRANSFER_PER_INPUT =
20_000`, wrapping requires `input_nullifiers.len() ≈ 9·10¹⁴` — far
beyond what serde can practically deserialize, so this is not
exploitable in isolation. It is, however, **inconsistent** with the
neighbouring estimators in `parallel.rs:1018-1021`, which already use
the saturating form:

```rust
crate::GAS_USER_OP.saturating_add(tx.call_data.len() as u64 * 16)
```

A wrap here would result in the fee controller charging a small
amount for an enormous transaction. Saturating to `u64::MAX` is the
correct conservative behaviour for any fee path.

**Fix:**

```rust
pub fn estimate_private_transfer_gas(tx: &PrivateTransferTx) -> u64 {
    GAS_PRIVATE_TRANSFER_BASE
        .saturating_add(
            GAS_PRIVATE_TRANSFER_PER_INPUT
                .saturating_mul(tx.input_nullifiers.len() as u64),
        )
        .saturating_add(
            GAS_PRIVATE_TRANSFER_PER_OUTPUT
                .saturating_mul(tx.output_commitments.len() as u64),
        )
}
```

---

### Closed on inspection (re-checked, no action needed)

These were on the audit candidate list; reading the code shows they
are already defended. Recording so the next auditor does not redo the
work.

- **`crates/evaporchain-crypto/src/signatures.rs:98` — `MlDsaKeypair::from_bytes` unsafe pointer writes.**
  Already mitigates layout assumptions with (1) compile-time
  `size_of::<Keypair>() == PK+SK`, (2) runtime
  `kp.public.as_slice() == pk_bytes && kp.expose_secret() == sk_bytes`
  comparisons after the writes, and (3) a sign-verify canary
  roundtrip. A silent field-order swap by the upstream crate would
  fail check (2). Closed.
- **`crates/evaporchain-network/src/service.rs:1607-1662` — BlockSync inbound.**
  Rate-limited (`:1614`), capped to `MAX_SYNC_BATCH = 100` blocks on
  both serving (`:1623`) and receiving (`:141`,
  `validate_sync_response_structure`). Closed.
- **`crates/evaporchain-consensus/src/api.rs:11757` and `crates/evaporchain-node/src/api.rs:11887` — `_ => unreachable!()`.**
  Both match on `&tx` where `tx` was constructed as
  `Transaction::UpgradeContract(...)` literally seven lines above. The
  unreachable arm is genuinely unreachable. Closed.
- **`crates/evaporchain-execution/src/rewards.rs:400` — `(pool as u128 * stake as u128 / total_stake as u128) as u64`.**
  `u64 × u64` cannot overflow `u128`. The final `as u64` truncation is
  bounded by the invariant `stake ≤ total_stake` (since
  `total_stake = Σ stake_i`), so the quotient is ≤ `pool` which fits
  in `u64`. Closed.
- **WebSocket subscriber cap.** `DEFAULT_MAX_WS_SUBSCRIBERS = 4096`
  enforced at `crates/evaporchain-node/src/ws.rs:183`. Closed
  (matches Audit-N M11).

---

## Addendum — audit-2026-05-13 findings (M7, M12, M15, M16)

All four CLOSED in commit `83077c19`.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| M7 | HIGH | `crates/evaporchain-network/src/service.rs` (gossipsub config) | Amplification — gossipsub auto-forwarded payloads before app-level deserialization | **CLOSED** (`83077c19`) — `.validate_messages()` + `report_message_validation_result` gating on every gossipsub arm |
| M12 | HIGH | `crates/evaporchain-state/src/snapshot.rs` | State corruption — fast-sync restore left stale stakes/delegations/sentinel/nullifiers | **CLOSED** (`83077c19`) — `wipe_full_state_for_snapshot_restore` full clean-slate + atomic WriteBatch |
| M15 | MEDIUM | `crates/evaporchain-script/src/vm.rs` | DoS — `hash()` and `to_string()` builtins charged flat GAS_CALL=10 for O(n) work | **CLOSED** (`83077c19`) — `GAS_HASH_BASE=10 + GAS_HASH_PER_32B=1`; `estimated_to_string_bytes` recursive upper-bound |
| M16 | MEDIUM | `crates/evaporchain-consensus/src/tendermint.rs` | Memory / perf — `dag_round_states` grew unbounded on LRU branch eviction | **CLOSED** (`83077c19`) — cascade-prune `dag_round_states[victim]` in `prune_state_branches`; 2 regression tests |
| M17 | MEDIUM | `crates/evaporchain-consensus/src/tendermint.rs:5724` | Memory — `settled_refunds` grew chain-lifetime (~4 GB at 10M blocks) | **CLOSED** (this commit) — `.retain()` prunes entries older than `refund_window_blocks` on every commit; 1 regression test |

---

## Addendum — audit-2026-05-14 findings (M18, L10, L8)

CLOSED in commits `b57d5713` and `4a3b284b`.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| M18 | MEDIUM | `crates/evaporchain-entropic-slashing/src/lib.rs` | Economic invariant — Shannon-entropy direction undocumented; future refactor could silently flip slash magnitude | **CLOSED** (`b57d5713`) — 3 proptest monotonicity pins: uniform=max-slash, `[count,1]` non-increasing in count, smaller-gap >= larger-gap slash |
| L10 | LOW | `crates/evaporchain-consensus/src/tendermint.rs:3516` | Memory — `pending_reveals` buffer unbounded; reveal-spam between proposals grew it without limit | **CLOSED** (`b57d5713`) — `PENDING_REVEALS_CAP = 50 * MAX_TXS_PER_BLOCK`; `submit_reveal` drops at cap; 1 regression test |
| L8 | LOW | `crates/evaporchain-script/src/vm.rs` (`Op::EmitEvent`) | OOM / wrap — `topic_count` had no upper bound; `tc=usize::MAX` wraps `tc+1`; large `tc` causes OOM via `Vec::with_capacity` | **CLOSED** (`4a3b284b`) — `MAX_EVENT_TOPICS=64`; `checked_add` for `+1`; `ScriptError::Runtime` on violation |

### Items intentionally not in scope this round

- Doctrine-correctness of consensus / state-root paths (covered by
  `AUDIT_2026_05_06.md` + Coq proofs in `research/coq/`).
- WASM-crate unsafe blocks (covered by closed CRITICAL-1).
- Documentation drift (per user direction — "no document audit").

### Process notes

- Sweep covered: unsafe blocks outside crypto/wasm (none),
  `Vec::with_capacity` against external length (1 finding),
  `as u64` / `as usize` casts in fee paths (1 finding),
  hardcoded passphrases / dev defaults in source (none found),
  `await`-holding-tokio-`Mutex` (no `tokio::sync::Mutex` in hot
  paths — `std::sync` only, which cannot be held across `.await`),
  `todo!()` / `unimplemented!()` in production (none),
  `panic!()` / `unreachable!()` in HTTP handlers (re-checked,
  match-on-locally-constructed-variant is the only pattern).
- No fuzzer or sanitizer runs in this pass — pure source review.
- All `cargo check / test` runs deferred to Mini 1 per
  `feedback_no_local_builds.md`; this audit changed no source.

---

## Addendum — audit-2026-05-15 U-class findings (upgrade/migration security)

Sweep: protocol-version gating, genesis/migration overflow, hard-fork replay, upgrade transaction validation.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| U1 | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs` | Hard-fork replay — existing governance flags (`parent_acceptance_mode`, `lambda_fold_mode`, etc.) already gate behaviour behind explicit flip | **CLEAN** — no unguarded hard-fork logic |
| U2 | CLEAN | `crates/evaporchain-execution/src/executor.rs` | Upgrade-tx validation — `Transaction::UpgradeContract` checks deployer ownership + schema before applying | **CLEAN** |
| U3 | HIGH | `crates/evaporchain-consensus/src/tendermint.rs` (proposal handler) | Version bypass — no `MIN_SUPPORTED_PROTOCOL_VERSION` check; malicious proposer can submit `protocol_version: 0` to bypass all version-gated logic (new opcodes, governance flags, multi-parent blocks requiring `protocol_version >= 3`) | **CLOSED** (`8501469b` — `MIN_SUPPORTED_PROTOCOL_VERSION = 0` const; reject < min before `block_hash`) |
| U4 | CLEAN | `crates/evaporchain-state/src/snapshot.rs` | Snapshot restore — `wipe_full_state_for_snapshot_restore` atomically clears stale state before applying snapshot (M12 fix from `83077c19`) | **CLEAN** |
| U5 | CLEAN | `crates/evaporchain-cli/src/keygen.rs` | Key migration — EVPL format dispatch via `detect_bls_key_format` + passphrase file env-var (`EVAPORCHAIN_VALIDATOR_KEY_PASS_FILE`) handles old/new formats safely | **CLEAN** |
| U6 | CLEAN | `crates/evaporchain-types/src/genesis.rs` | Migration config — chain_params validated against real bounds (min_validator_stake, fee rates, commission ranges) | **CLEAN** |
| U7 | MEDIUM | `crates/evaporchain-types/src/genesis.rs:validate()` | Integer overflow — per-validator stake individually below max but aggregate can exceed `u64::MAX`; quorum threshold wraps to near-zero, making BFT trivially satisfiable | **CLOSED** (`8501469b` — `total_stake: u128` accumulator; error if `total > u64::MAX as u128`) |

---

## Addendum — audit-2026-05-15 V-class findings (DA/network validation security)

Sweep: vote equivocation, DA certificate verification, DA sampling proof verification, shard assignment manipulation, PoHA certificate authentication, proposal message authentication, network message replay.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| V1 | CLEAN | `crates/evaporchain-node/src/main.rs:7059` | DA sampling — `DASampler::verify_proof()` + `blake3::hash == shard.hash` both called before counting valid samples; `da_valid_sample_count` reset to 0 at each new block's sample request (lines 4615/5643) | **CLEAN** |
| V2 | HIGH | `crates/evaporchain-consensus/src/tendermint.rs` (`ConsensusMessage::Proposal`) | Gossip-level forgery — Proposal message had no BLS signature; any peer could inject a Proposal with the expected `proposer_id` and honest validators would vote on the forged block | **CLOSED** (`a866d3aa` — `bls_signature: Option<Vec<u8>>` added; proposer signs over `bls_vote_message(chain_id, h, r, hash, "proposal")`; receiver verifies before equivocation check; None warned but accepted during migration window) |
| V3 | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs:7754` (`verify_da_certificate`) | DA certificate BLS verification — `cert.verify_signatures()` called; supermajority, attestation count, block-number window, and BLS aggregate all validated | **CLEAN** (N4 closure `83077c19` confirmed) |
| V4 | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs:5390,5515` | Equivocation detection — prevote and precommit equivocation both detected; `sanov_slash_equivocation` fired; both block hashes tainted; `ConsensusAction::SlashValidator` emitted | **CLEAN** |
| V5 | CLEAN | `crates/evaporchain-sharding/src/shard_assignment.rs:42` | Shard assignment — deterministic on `object_id` (power-of-2 mask); no validator-influenceable input | **CLEAN** |
| V6 | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs:5360,5485` | Prevote/Precommit BLS authentication — both message types verified against validator BLS pubkey before processing | **CLEAN** |
| V7 | DESIGN | `crates/evaporchain-da/src/poha.rs` | PoHA aggregate signature — `aggregate_signature` field stored but `bls_sign` never called on the production path (`main.rs` passes `vec![]`); no verifier exists. Design incompleteness, not a bypass attack | **ACKNOWLEDGED** — full PoHA BLS aggregation deferred to PoHA V2 sprint; no forgery possible since production also writes empty sigs |

---

## Addendum — audit-2026-05-15 W-class findings (wallet/key management security)

Sweep: BLS key loading entry point, passphrase handling, key zeroization, secret file permissions, key rotation, mnemonic derivation, KeyAnnounce Proof-of-Possession, EVPL format coverage.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| W1 | MEDIUM | `crates/evaporchain-node/src/main.rs:2985,2990` | Format drift — plaintext BLS fallback paths wrote raw `sk` bytes without EVPL magic prefix; `detect_bls_key_format` classified them as `BLS_LEGACY` and emitted deprecation warnings on next load | **CLOSED** (`94ca9873` — both fallback paths now call `format_plaintext_for_disk(sk)` before `write_secret_file`) |
| W2 | MEDIUM | `crates/evaporchain-node/src/main.rs:193` (`write_secret_file`) | TOCTOU — `std::fs::write()` created file at umask (0644), then `set_permissions(0o600)` raced against same-host attacker reading the key | **CLOSED** (`94ca9873` — `OpenOptions::new().mode(0o600)` on Unix creates file 0600 atomically; never world-readable) |
| W3 | CLEAN | `crates/evaporchain-crypto/src/signatures.rs:424` | BLS key zeroization — `BlsSecretKey` has explicit `Drop` calling `self.0.zeroize()`; field-level drop propagates correctly through `BlsKeypair` | **CLEAN** |
| W4 | CLEAN | `crates/evaporchain-crypto/src/bls_key_store.rs` | BLS format detection — `detect_bls_key_format` is the single entry point; no alternate path loads raw bytes without format dispatch | **CLEAN** |
| W5 | CLEAN | `crates/evaporchain-node/src/main.rs:2962`, `crates/evaporchain-crypto/src/bls_key_store.rs:59` | Passphrase handling — sourced only from `EVAPORCHAIN_VALIDATOR_KEY_PASS_FILE` or `EVAPORCHAIN_VALIDATOR_KEY_PASS`; never logged, printed, or stored in temp files | **CLEAN** |
| W6 | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs:3815` | Key rotation — continuity proof (`bls_pop_old`) verified against old pubkey under rotation DST; grace window retains old key for `prev_key_expiry_epoch` blocks | **CLEAN** |
| W7 | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs:4682` | KeyAnnounce PoP — `BlsVerifier::verify_proof_of_possession` enforced before accepting announced BLS key; rogue-key attack prevented | **CLEAN** |
| W8 | CLEAN | `crates/evaporchain-node/src/main.rs` | Mnemonic derivation — validator consensus keys generated via `BlsKeypair::generate()` (secure random); no BIP39 mnemonic in the consensus key path | **CLEAN** |

---

## Addendum — audit-2026-05-15 X-class findings (EvaporScript execution security)

Sweep: gas metering overflow, step-limit bypass, external-call gas accumulation, stack overflow, memory bounds, arithmetic safety, contract re-entrancy, storage isolation, string/bytes bounds, map/list caps.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| X-G1 | CRITICAL | `crates/evaporchain-script/src/vm.rs:174` (`charge_gas`) | Gas overflow — `gas_used += cost` wrapped on overflow, resetting counter to near-zero and allowing unbounded execution past gas limit | **CLOSED** (`43ece4f5` — `checked_add`; returns `GasLimitExceeded` on overflow before compare) |
| X-G2 | HIGH | `crates/evaporchain-script/src/vm.rs:227` (step counter) | Step bypass — `step_count += 1` wrapped on overflow, resetting below `MAX_STEPS=10M` and bypassing the instruction limit | **CLOSED** (`43ece4f5` — `checked_add`; returns `StepLimitExceeded` on overflow) |
| X-G3 | MEDIUM | `crates/evaporchain-script/src/vm.rs:871` (external call) | Gas accumulation — callee's `gas_used` added via unchecked `+=` after `call_external` returns; malicious callee could cause overflow | **CLOSED** (`43ece4f5` — `checked_add` + re-check against `gas_limit`) |
| X-O2 | MEDIUM | `crates/evaporchain-script/src/parser.rs:271` (`read_string`) | String literal OOM — no length cap on string literal tokenisation; multi-MiB literal in single token allocated unbounded memory | **CLOSED** (`43ece4f5` — `MAX_STRING_LITERAL = 65536` bytes; `Parse` error on excess) |
| X-O1 | ACKNOWLEDGED | `crates/evaporchain-script/src/vm.rs:532` | u64→usize array index cast — safe on all 64-bit targets; not patched | **ACKNOWLEDGED** — EvaporChain targets 64-bit only; `u64 as usize` is lossless |
| X-R1 | CLEAN | `crates/evaporchain-script/src/vm.rs:129,834` | Contract re-entrancy — `in_lifecycle_hook` flag + `ContractCallRouter::active_calls` set block re-entrant calls | **CLEAN** |
| X-S1 | CLEAN | `crates/evaporchain-script/src/vm.rs:120` | Storage isolation — per-contract `state: HashMap<String, Value>` passed by value; cross-contract calls snapshot, not reference | **CLEAN** |
| X-M1 | CLEAN | `crates/evaporchain-script/src/vm.rs:515,650` | Map/array caps — `MAX_ARRAY_SIZE=10000`, `MAX_MAP_ENTRIES=10000`, `MAX_MEMORY_BYTES=4MiB` all enforced | **CLEAN** |
| X-A1 | CLEAN | `crates/evaporchain-script/src/vm.rs:285,312,319,329` | Arithmetic overflow — Add/Sub/Mul/Div/Mod all use `checked_*` with `ScriptError` on overflow | **CLEAN** |
| X-E1 | CLEAN | `crates/evaporchain-script/src/lib.rs:198` | Deploy-time bytecode validation — opcode count ≤ 65536; all jump targets in-range validated at deploy | **CLEAN** |

---

## Addendum — audit-2026-05-15 Y-class findings (governance parameter validation)

Sweep: governance parameter bounds, PID fee controller clamp safety, reward/slash accounting, emission schedule, delegation invariants, epoch reset, MEV refund, economic invariants.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| Y-FEE-001 | MEDIUM | `crates/evaporchain-execution/src/lib.rs` (`validate_param_value` + `apply_governance_params`) | Governance clamp panic — `base_fee_floor` and `base_fee_ceiling` accepted any u64 with no upper bound and no cross-check. Setting floor > ceiling → `PidFeeController::fees.rs:158` `.clamp(min, max)` panics (Rust panics when min > max), crashing node at block production | **CLOSED** (`02c0bd6d` — (1) `validate_param_value` rejects values > 1_000_000_000_000; (2) `apply_governance_params` resets both to defaults (100, 1_000_000) with `warn!` if floor > ceiling after DB load; (3) 3 new bounds tests) |
| Y-FEE-002 | FALSE POSITIVE | `crates/evaporchain-execution/src/lib.rs:379` (`GOVERNABLE_PARAM_KEYS`) | `fee_response_ppm` not in allowlist — governance cannot set it; no attack surface | **NOT A FINDING** — `fee_response_ppm` not in `GOVERNABLE_PARAM_KEYS`; only `block_gas_limit`, `base_fee_floor`, `base_fee_ceiling`, `target_gas_utilization` are governance-mutable |
| Y-R1 | CLEAN | `crates/evaporchain-execution/src/economics.rs` (reward/slash accounting) | Reward math saturating — all staking reward and slash arithmetic uses `saturating_add`/`saturating_sub`; no panic path | **CLEAN** |
| Y-E1 | CLEAN | `crates/evaporchain-execution/src/economics.rs` (emission schedule) | Emission cap enforced — total supply growth gated behind energy-decay formula; no mint-without-bound path | **CLEAN** |
| Y-D1 | CLEAN | `crates/evaporchain-execution/src/economics.rs` (delegation invariant) | Delegation accounting consistent — delegated stake tracked symmetrically in both delegator and validator records | **CLEAN** |
| Y-MEV | CLEAN | `crates/evaporchain-consensus/src/tendermint.rs` (MEV refund) | MEV refund path — Crooks-MEV settlement uses checked arithmetic throughout; no overflow in refund accumulation | **CLEAN** |

---

## Addendum — audit-2026-05-15 Z-class findings (serialization, codec, zero-copy, WAL)

Sweep: WAL length encoding, network deserialization size caps, schema version gating, zero-copy unsafe blocks, recursive codec depth, consensus-message pre-decode signature ordering.

| ID | Severity | Site | Class | Status |
|---|---|---|---|---|
| Z-WAL-001 | MEDIUM | `crates/evaporchain-state/src/wal.rs:103` (`begin_block`) | WAL length field — `payload.len() as u32` silently truncates if mutation batch > 4GB; BLAKE3 checksum would catch corruption on read, but write failed silently | **CLOSED** (`bfb6532a` — `try_into::<u32>()` returns `io::Err` explicitly if payload > 4GB) |
| Z-NET-001 | CLEAN | `crates/evaporchain-network/src/service.rs:1759,1784,1798` | Network deserialization — size caps enforced (`MAX_GOSSIP_MESSAGE_SIZE=4MB`, `MAX_CONSENSUS_MESSAGE_SIZE=512KB`) *before* `serde_json::from_slice`; bounded allocation | **CLEAN** |
| Z-VER-001 | CLEAN | `crates/evaporchain-network/src/service.rs`, `crates/evaporchain-consensus/src/tendermint.rs` | Schema version drift — `protocol_version` checked in consensus handler via `MIN_SUPPORTED_PROTOCOL_VERSION` (U3 fix, same session); network layer correctly forwards raw bytes and lets consensus gate | **CLEAN** |
| Z-DAG-001 | CLEAN | `crates/evaporchain-consensus-types/src/tendermint.rs:2146` | Recursive DAG traversal — depth limit `1_000_000` enforced in Light Cone ancestry walk; no stack overflow | **CLEAN** |
| Z-UNS-001 | CLEAN | `crates/evaporchain-crypto/src/signatures.rs:72–126` | ML-DSA `unsafe` block — guarded by compile-time `size_of` assertion + runtime field-match check + sign-verify canary; layout mismatch caught before any UB | **CLEAN** |
| Z-SIG-001 | CLEAN | `crates/evaporchain-network/src/service.rs:1809` | Consensus message signature ordering — raw bytes forwarded to consensus engine which decodes + verifies; size cap (512KB) prevents allocation bombs in the decode step | **CLEAN** |

---

## Addendum — audit-2026-05-15 MCP + Bridge + Oracle sweep

Sweep: MCP server (26 tools, 13 resources, 6 prompts), ETH bridge (Solidity + Rust), BFT oracle.

### ETH Bridge findings (all CLEAN / FALSE POSITIVE)

| ID | Severity | Site | Assessment |
|---|---|---|---|
| B-REPLAY | FALSE POSITIVE | `ethereum-bridge/contracts/src/EvaporHeaderInbox.sol`, `EvaporationDispatcher.sol` | Agent claimed missing replay protection post-reorg. ACTUAL: L1 state reverts atomically — `h.fired`, `_headers[N]`, `l1AcceptedAt[N]` all revert together; 12-block finalization gate prevents dispatch during any live reorg window. BFT quorum (2/3+ stake) required to submit a header. No exploit path. |
| B-DEPTH | DESIGN | `BridgeConstants.sol` (`MIN_FINALIZATION_DEPTH = 12`) | 12 blocks (~2.5 min post-merge) is industry standard; deeper depth is an operational trade-off, not a code bug. Acknowledged. |
| B-CEI | CLEAN | `EvaporationDispatcher.sol:185` | `h.fired = true` set BEFORE external call — correct CEI order; no reentrancy. |
| B-MMR | CLEAN | `lib/MmrInclusion.sol:65–95` | MMR bagging verified left-to-right, matching Rust producer. |

### Oracle findings (all CLEAN / DESIGN)

| ID | Severity | Site | Assessment |
|---|---|---|---|
| O-TWAP | CLEAN | `crates/evaporchain-oracle/src/consensus.rs:104–135` | TWAP requires ≥3 entries (line 116 guard); `finalize()` stores `None` for first 2 rounds. Primary oracle value is always the BFT quorum median — TWAP is an optional supplement. No manipulation path. |
| O-FRESH | DESIGN | `crates/evaporchain-oracle/src/lib.rs:111–118` | Default 300s freshness; `price_feed()` preset uses 120s. Callers responsible for choosing appropriate config. Acknowledged. |
| O-QUORUM | CLEAN | `crates/evaporchain-oracle/src/consensus.rs:220–244` | BLS signature per validator, quorum-gated, median + outlier rejection. Single-validator manipulation impossible under honest quorum. |

### MCP server findings

| ID | Severity | Site | Status |
|---|---|---|---|
| MCP-AUTH-001 | HIGH | `crates/evaporchain-mcp/src/main.rs` | **CLOSED** (`b2123dc6` — startup probe fires POST /api/faucet without token on startup; warns loudly if node returns non-401, catching node-side auth misconfiguration) |
| MCP-INJECT | MEDIUM | `crates/evaporchain-mcp/src/resources.rs:117`, `prompts.rs:141–306` | Blockchain data embedded as JSON into AI context; JSON-structural injection impossible; semantic prompt injection requires compromised node API data. ACKNOWLEDGED — defence-in-depth would add a `TRUSTED_NODE_DATA:` wrapper but local node trust boundary is acceptable. |
| MCP-INPUT | CLEAN | `crates/evaporchain-mcp/src/validation.rs:94–208` | Five domain validators gate all tool inputs before use; range/format/overflow all covered. |
| MCP-CONSENT | CLEAN | `crates/evaporchain-mcp/src/tools.rs:156–211` | All 5 write-tools carry `requiresConsent: true` + description prefix warning. |
| MCP-RATE | CLEAN | `crates/evaporchain-mcp/src/rate_limit.rs` | Per-tool rate limits (write: 10/60s, compute: 30/60s, read: 300/60s). |
| MCP-SCOPE | CLEAN | `crates/evaporchain-mcp/src/tools.rs:396–656` | All tools single-purpose; no hidden parameters or scope creep. |
