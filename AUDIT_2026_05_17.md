# EvaporChain comprehensive audit — 2026-05-17

> **STATUS AS OF 2026-05-28: most findings closed in-tree.** Spot-checks against current main:
> CR-1 (energy_verkle DST), CR-2 (path_indices binding), STATE-2 (9 RocksDB stubs), SBA-1 (commit-reveal binding), H-1 (VRF chain-id-scoping via #407), H-2 (address-derivation helper via #413), H-4 (BLS PoP via #414), Q1/Q6/Q9/Q10/Q13/A4/A5/A8 (via #401, #408, #411, #412, #416), and the 6 P0 launch-blockers from `EVAPORCHAIN_AUDIT.md` (via #469) all show closure markers or implementations in source. This doc is preserved as the **point-in-time audit snapshot** — it does NOT reflect open work as of today. Search the codebase for `audit 2026-05-17` comments to find the actual closure sites, and consult `SESSION_PROGRESS.md` / `CHANGELOG.md` for the running closure log. The 2026-05-24/25 marathon merged 14 of the audit-PR closures; #461 + #469 closed the remaining two on 2026-05-28.

7 parallel auditing sweeps covering crypto + post-quantum, consensus + DA, energy + conservation, invention-stack ↔ doctrine alignment, execution + EvaporScript, RPC + node + paymaster + MCP, and formal-proof + spec alignment. Each agent grepped prior closure ledgers (`AUDIT_2026_05_11.md` + addenda + memory `evaporchain_audit_*` entries) before flagging — every finding below is either **NEW** or a documented regression of a prior closure.

Workspace HEAD audited: cluster pull at session start, before any new commits.

## Headline tally

| Bucket | Count |
|---|---|
| CRITICAL | 9 |
| HIGH | 14 |
| MEDIUM | 25 |
| LOW | 13 |
| Spec / proof / doc drift findings | 11 |

**Top-of-stack drive order (do these before anything else):**

1. **CR-1** — fix the energy-verkle test that's red on HEAD before any other work.
2. **CR-2 + CR-3** — same crate, same DST mismatch class, same patch shape.
3. **Q1 + Q2 + Q3** — DA-cert single-key forgery chain (consensus + DA).
4. **A1 + A2 + A3** — RPC wallet impersonation across `wallet_sign_tx`, `wallet_submit_tx`, `post_settle_demurrage`, 5 Singh-pool handlers.
5. **STATE-2** — RocksDB stubs 9 governance + snapshot trait methods. With `conservation_enforcement` now defaulting to `"enforce"` (CLAUDE.md update at commit 76d95590), this directly compounds the trust story.
6. **SBA-1** — `sealed_bid_auction.es` has zero commit-reveal binding at the contract level.

---

## 1. Crypto + post-quantum (agent: crypto)

**Baseline broken**: `cargo test -p evaporchain-crypto --lib` is **NOT GREEN on HEAD**. `energy_verkle::tests::test_proof_verifies` panics with `proof failed for key 0`. Regression introduced by commit `b5959a05` ("H2 DST domain separation"). The fix is half-applied.

| ID | Sev | Site | Class |
|---|---|---|---|
| CR-1 | **CRITICAL** | `crypto/src/energy_verkle.rs:208-229` vs `:759-823` | `EnergyNode::hash` has no DST but `EnergyVerkleTrie::verify` reconstructs with `VERKLE_LEAF_DST` / `VERKLE_INTERNAL_DST`. Verify breaks against any non-trivial trie root. **Test red on HEAD.** ✅ **CLOSED 2026-05-28 spot-check:** `EnergyNode::hash` at `energy_verkle.rs:215` now emits the `VERKLE_LEAF_DST` / `VERKLE_INTERNAL_DST` prefix, matching `verify`. |
| CR-2 | **CRITICAL** | `crypto/src/verkle.rs:425-505` + `energy_verkle.rs:759-823` | `verify` never checks `proof.path_indices[i] == proof.key[i]`. Combined with `leaf_hash = [0u8; 32]` for non-existence proofs (scalar zero = identity contribution), attacker forges non-existence proof for an EXISTING key by routing path through an empty trie slot. ✅ **CLOSED:** explicit `// CR-2 (audit 2026-05-17)` loop at `verkle.rs:461` rejects `path_indices[level] != key[level]`. |
| CR-3 | **CRITICAL** | `crypto/src/energy_verkle.rs:1086-1158` | `verify_multi` reconstructs WITHOUT DST (consistent with the producer) while `verify` reconstructs WITH DST. The two sibling verifiers disagree on canonical hash semantics. One of them is wrong. Both ship. ✅ **CLOSED:** producer + both verifiers now share the same DST path via `EnergyNode::hash`. |
| H-1 | HIGH | `crypto/src/vrf.rs:203-217`; callers `consensus/tendermint.rs:5351, 6856, 15866, 16005` | `leader_vrf_input(height, round)` is NOT chain-id-scoped. K4 added chain_id to BLS vote messages but VRF input wasn't touched. Same-key cross-chain leader-claim replay surface. ✅ **CLOSED via PR #407 (`9568a890`).** |
| H-2 | HIGH | `execution/src/lib.rs:3965, 7387, ...` (∼10 sites) | Address = `blake3(public_key_bytes)` with **no DST**. I1 binding closure didn't add domain separation. Hardening gap, not exploit today. ✅ **CLOSED via PR #413 (`570c3be2`)** — `address-derivation helper + L0-B/C doctrine`. |
| H-3 | HIGH | `crypto/src/accumulator.rs:250-283` | `MMRProof.mmr_size` plumbed through but never validated against external commitment. Verifiers denied the cheap `proof.mmr_size == known_size` pre-check (SUB-N1 class). ✅ **CLOSED** — `accumulator.rs:251` carries `// H-3:` marker and validates `mmr_size` structurally (derives leaf_count, validates leaf_index bound, peak count via popcount, sibling count via heights) before any hash work. Stronger than the audit's suggested external pre-check. |
| H-4 | HIGH | `crypto/src/bls_portable.rs:62-118` | `aggregate_verify` sums G1 keys with no per-key PoP at verify site. Validator path is gated at registration; non-validator callers (browser dApps, light clients, indexers) inherit rogue-key exposure. ✅ **CLOSED via PR #414 (`d0bf414f`)** — `bls_portable PoP precondition + M-4/M-5 Poseidon scope docs`. |
| M-1 | MED | `crypto/src/secret_file_store.rs:54` | Argon2id `t=3` here vs `t=4` in `bls_key_store.rs:110` (GEN-N5). Same threat model, different cost. ✅ **CLOSED:** `secret_file_store.rs:59` uses `Params::new(64*1024, 4, 1, …)` — t_cost=4 matches GEN-N5. |
| M-2 | MED | `crypto/src/secret_file_store.rs:82, 140` | Argon2id-derived key not wrapped in `Zeroizing<>`. CLI-KDF-001 closed this for EVK1 but didn't propagate to EVKV. ✅ **CLOSED:** explicit `// M-2 (audit 2026-05-17)` comment + `Zeroizing::new(kdf(…))` at lines 90 and 148. |
| M-3 | MED | `crypto/src/verkle.rs:177-200`; `energy_verkle.rs:236-248` | `VerkleProof.commitments` field filled by `prove` but never read by `verify`. Wire bloat + auditor footgun. ✅ **CLOSED:** both `verify` (verkle.rs:447) and `energy_verkle::verify` (energy_verkle.rs:795) now read `proof.commitments.len()` as a dim-consistency gate. |
| M-4 | MED | `crypto/src/hash.rs:217-242` | Poseidon sponge has no IV, no capacity tag, no padding DST, no length prefix. Empty input collapses to `Fp::ZERO`. Used only in ZK paths today; harden or rename. ✅ **CLOSED (deliberate)** — hash.rs:216+ now documents this as an intentional in-circuit constraint and explicitly recommends `blake3_hash` with a DST for any non-circuit caller. PR #414 included the scope-docs. |
| M-6 | MED | `bridge/contracts/src/lib/HashToCurve.sol:14-25` | Doc says POP DST, code uses NUL DST. Mutually exclusive — doc lies. ✅ **CLOSED:** path now correctly `ethereum-bridge/contracts/src/lib/HashToCurve.sol`. DST = `BLS_SIG_BLS12381G2_XMD:SHA-256_SSWU_RO_NUL_` with comment "`_NUL_` application tag for non-PoP signatures" — doc + code agree. |
| L-1..L-5 | LOW | various | minor unwraps, naming drift, no curve-membership pre-check on EIP-2537 bridges |

**Spec compliance:** FIPS-204 ML-DSA ~95%, BLS12-381 (RFC 9380) ~95%, EIP-2537 / RFC 9380 hash-to-curve ~98%, Verkle (EIP-6800 / Kuszmaul 2019) **~50%** until CR-1+CR-2+CR-3 are fixed.

**CLEAN:** ML-DSA from_bytes unsafe block (size assert + canary), BLS PoP DST distinct from sig DST, hybrid verifier non-short-circuit AND (Crypto-3), zeroize-on-drop, EIP-2537 G1/G2 encoder coordinate-order (ZCash→EIP), MMR nullifier leaf+node DSTs (H4 closure), `bytes_to_scalar` post-mask (L1 closure), Argon2id OWASP-2026 in `bls_key_store` (GEN-N5), `detect_bls_key_format` magic-byte dispatch (Crypto-6), `RandomnessBeacon::ingest` order-sensitive chaining.

---

## 2. Consensus + DA (agent: consensus)

> **Section closure as of 2026-05-28:** all 13 Q-findings carry explicit `// Q{N} (audit 2026-05-17):` markers in source (verifiable via `git grep "Q[0-9]\\+ (audit 2026-05-17)" crates/`). Q1+Q2+Q3 collectively closed by the new `verify_signatures_bound(registered)` path (DA-001 / #469); per-finding closure sites: Q2 `certificate.rs:70` (dedup), Q3 `certificate.rs:89` (stake bound in signed message), Q4 `certificate.rs:49` (strict `> 2T/3`), Q5 `tendermint.rs:2502` (stake-weighted antichain quorum), Q6 PR #401 (canonical sample seed), Q7 `bridge.rs:146-148` + `tree_size`/`leaf_index` validation, Q8 superseded by `verify_signatures_bound` call at `tendermint.rs:8256`, Q9/Q13 PR #412 (Q9 assert + Q13 slash short-circuit), Q10 PR #416 (NMT empty sentinel), Q11 documented as unmodeled-but-safe TLA seam, Q12 chain-id refusal added to network service.

| ID | Sev | Site | Class |
|---|---|---|---|
| Q1 | **CRITICAL** | `consensus/src/tendermint.rs:8019-8104` (`verify_da_certificate`) | `cert.total_stake` and per-attestation `att.stake` attacker-supplied; never cross-checked against `validator_set.total_stake()` nor registered per-validator stake. **Single-key DA cert forgery:** attacker generates one BLS keypair, signs the required message, ships cert with `total_stake=0, attested_stake=0, attestations=[one_valid_sig]` → `is_supermajority` returns `0*3 >= 0*2 = true`. |
| Q2 | **CRITICAL** | `da/src/certificate.rs:56-93` (`DACertificate::verify_signatures`) | No dedup by `validator_id`. Ship one cert with 100 copies of the same valid attestation → `recomputed_stake = 100 × stake`. Bypasses gossip-RX dedup at `tendermint.rs:4899`. |
| Q3 | **CRITICAL** | `da/src/certificate.rs:147-178` (`create_attestation`) | BLS signed message excludes the `stake` field. Any old attestation can have its `stake` rewritten to `u64::MAX/4` and signature still verifies. Compounds Q1+Q2. |
| Q4 | HIGH | `consensus/tendermint.rs:4201-4217` + `da/certificate.rs:46-48` + `da/poha.rs:149-151` | Tendermint safety requires *strict* `> 2T/3` but code uses `>=`. TLA spec's `|Faulty|*3 < |Validators|` STRICT ASSUME saves the model; runtime has no equivalent. Byzantine actor with stake exactly `T/3` (`T` divisible by 3) can produce two disjoint quorums for different blocks at same height. `finality.rs:58` uses correct strict `>`. |
| Q5 | HIGH | `consensus/tendermint.rs:2463-2493` (`try_finalize_antichain`) | Antichain finalization is count-weighted (`precommit_count >= 2*f+1`). H11 closed this for main path; the antichain path still has the bug. Small-stake majority can finalize over large-stake minority. |
| Q6 | HIGH | `consensus/tendermint.rs:7917-7923` + `consensus-types/tendermint.rs:7432-7438` | 2D-DA sample seed built as `b"da-2d-sample" \|\| block.number \|\| my_id` — bypasses canonical `DASampler::build_da_sample_seed_v1(block_number, data_root, validator_id)` from `da/sampling.rs:380`. H7 Stage A's `data_root` binding silently inactive on consensus DA-sampling hot path. Adversarial proposer can grind `data_root` after seeing public (height, validator_id). |
| Q7 | HIGH | `consensus/src/bridge.rs:126-143` (`StateProof::verify`) | Unsafe sorted-Merkle: no leaf-index, no tree-size, no DST. Tree-size confusion class (parallel to SUB-N1). |
| Q8 | HIGH | `consensus/tendermint.rs:8097` | `verify_da_certificate` calls `cert.verify_signatures()` instead of `verify_signatures_with_active(...)`. M4 closure's helper exists but main path never invokes it — stale certs with jailed/exited signers accepted. |
| Q9 | MED | `consensus/tendermint.rs:7394-7411` (`bls_vote_message`) | Phase enum enforcement is `debug_assert!` only (stripped at release); phase string not length-prefixed. |
| Q10 | MED | `da/src/namespace.rs:90-93, 123-129` | `is_empty()` checks `hash == [0u8; 32]` — practical-impossible but theoretically reachable hash collision turns empty sentinel ambiguous. |
| Q11 | MED | `consensus/tendermint.rs:7253-7268` | Round-state reset at `MAX_ROUNDS_PER_HEIGHT` wipes vote tally — not modeled in TLA. Safe today via lock-aware vote logic but unmodeled seam. |
| Q12 | LOW | `network/src/service.rs:1394-1401` | Empty `chain_id` falls back to unscoped legacy gossipsub topics. Mainnet should refuse to start. |
| Q13 | LOW | `consensus/tendermint.rs:2103-2134` | Equivocation slash amplification — each duplicate vote re-runs KL divergence on receiver. Cap-once-per-(height, round, validator) closes it. |

**TLA ↔ impl drift (8 distinct properties)** including weaker spec on lock rule (D2), unmodeled antichain (D3), unmodeled round-state wipe (D4), count-vs-stake DA quorum drift (D5), unmodeled key rotation / jailing (D7), unmodeled max-rounds reset (D8).

**CLEAN:** MCC integer determinism, encrypted mempool AAD (PRIV-N5/N6), equivocation BLS-verify-before-record, K4 chain_id binding in BLS vote, DA cert block-number window, block-timestamp future bound, gossipsub validate-before-relay (M7), mDNS off + chain-id-scoped topics, Sybil disconnect, NMT DST prefixes (NN2), PoHA cert hash DST + 11-field coverage (NN3), Bell-Beacon v2 chain_id + prev_block binding (H2 prior).

---

## 3. Energy decay + conservation (agent: energy)

| ID | Sev | Site | Class |
|---|---|---|---|
| L0-A | HIGH | `lambda-fold/src/nova_path.rs:153-157` | Nova IVC running-total decay uses the **FIRST object's** half_life (or hard-coded fallback `100`) instead of `ChainLambda`. Sister `fold.rs:51` does it correctly — `nova_path` missed the doctrine fix. |
| CONS-A | HIGH | `execution/lib.rs:3808`, `parallel.rs:2406`, `block_stm.rs:2233`, `consensus/tendermint.rs:6081`, `consensus-types/tendermint.rs:5712`, `node/api.rs:4227`, `consensus/api.rs:3980` | All 7 conservation gate call sites hard-code `ChainLambda::default_genesis()`. `lambda.rs` doc claims governance can rotate λ; no governance read path exists. |
| GHOST-A | CRITICAL (paper drift) | `state/src/refresh.rs:82-146` | Resurrection removes ghost record but **never marks the MMR nullifier consumed**. Paper 1 §3.4 Inv-4 ("MMR entry is consumed, marked with a nullifier hash") not enforced. Runtime double-resurrect blocked by RocksDB ghost-record removal alone. Paper amendment or MMR-consume implementation. |
| GHOST-B | MED | `state/src/refresh.rs:118-119`; `execution/lib.rs:1432-1454` | `execute_refresh` has zero owner-vs-sender check. Anyone can resurrect any ghost (paying the fee). Object reverts to original owner → no theft, but griefing: forced state inflation, lifecycle hooks fire, decay-curve obligations on unwilling owner. |
| L0-B | MED | `types/src/lib.rs:2052-2056` | `MEV_INCLUSION_HALF_LIFE_BLOCKS=4` is a second hard-coded decay constant used directly. Doctrine permits unit-converted boundary constants but no conversion exists. |
| L0-C | MED | `types/src/genesis.rs:87-89, 199-204` | `reward_half_life` is a third decay constant. Probably intentionally distinct from λ, but doctrine says single-λ — needs explicit enumeration. |
| TOK-A | MED | `consensus/src/api.rs:13117-13127` | `DeployedToken::tick_decay` decays per-holder balances independently; floor-division truncates → silent supply destruction up to N units per tick. Contract-template drift from Paper 1 chain-wide invariant. |
| MMR-MEM | LOW | `state/src/db.rs:807-819` | `prune_before_height(epoch-1000)` makes ghosts past cold-storage horizon permanently unresurrectable. Paper 1 §3.2 explicitly carves this out. CLEAN, flagged for completeness. |
| VM-SHR | LOW | `state/src/decay_curves.rs:106, 163-167` | `DecayCurve::Custom` VM has `OP_SHR` on user-stack values. Borderline — matches `energy_at_epoch` shape (cap at 64, saturating). Filed for awareness. |

**Coq ↔ Rust `energy_at_epoch` alignment:** byte-for-byte aligned across 7 steps. Zero-half_life guard, full_halvings, remainder, 64-halving cutoff, linear interpolation with u128 widening, saturating-sub. Coq uses `nat` (unbounded); Rust uses `u64` saturating — the u64 path is **more conservative** than the nat model. **CLEAN.**

**Paper drift:** Paper 1 §3.4 Inv-4 vs `refresh.rs` (GHOST-A). Paper 1 implied resurrection-authorized-by-owner vs `RefreshTx` (GHOST-B). Paper 2 §3.5 doesn't cross-reference Paper 1's cold-storage carve-out (soft drift).

**CLEAN:** Layer-0 invariant (no raw `>>` on energy outside types, verified across 73 callers), `BatchUndoLog` snapshots all 10 buckets (M10 intact), Refresh pool ledger preserved by proptest, Fee controller Lyapunov u128 squaring + i128 signed-diff overflow-safe (LYAP-001 intact), Decay-Lamport pure logical clock.

---

## 4. Invention-stack ↔ doctrine alignment (agent: invention-stack)

| ID | Sev | Site | Class |
|---|---|---|---|
| INV-HIGH-1 | HIGH | `execution/src/mera_integration.rs`; called at `execution/lib.rs:3857`, `block_stm.rs:2217`, `parallel.rs:2454` | **MERA computed on every block's hot path** despite doctrine §A1.8 FAIL → VERKLE verdict + CLAUDE.md "Do not treat `evaporchain-mera` as a production crate". No consumer reads `BlockExecutionResult.mera_commitment` (verified by grep across consensus + node), so it's wasted cycles + lying metadata, not exploit. |
| INV-HIGH-2 | HIGH | `crates/evaporchain-mera/Cargo.toml:7` | Crate's own package description claims `§A1.8 PASS` — gate FAILED. Crate's `lib.rs` head correctly says FAIL. Two artifacts in the same crate disagree. Visible in `cargo metadata` / future crates.io publication. |
| INV-MED-3 | MED | `causal-chsh/src/lib.rs:55-71`; `causal-chsh/Cargo.toml:7` | Both still say `⚠ GATED — pending empirical gate`. Gate has PASSED twice (200-block + 3K-block confirm). Stale claim in the conservative direction. |
| INV-MED-4 | MED | `light-cone/src/lib.rs:10-18` (honest); doctrine §4.1 #1 (overclaim) | Crate honestly documents "Production status: read-only observability layer ... 'Soul of the chain' framing describes the *intent*, not the shipped behaviour." Doctrine itself overclaims. Either doctrine downgrade or fork-choice promotion. |
| INV-MED-5 | MED | `total-evaporscript/src/lib.rs`, `cap-decay-vm/src/lib.rs`, `dp-native-vm/src/lib.rs` | All three Tier-2 VMs cite §-ref only in `Cargo.toml` description, not `lib.rs` head doc. Tier-2 has no equivalent of Tier-0's mandatory citation rule, so style drift not violation. |
| INV-MED-6 | MED | `decay-lamport`, `fee-controller`, `llsa`, `sentinel`, `tombstone` | Lack standalone `tests/e2e.rs`. Doctrine "every primitive ships with an e2e test against non-trivial fixture" loosely satisfied via inline `press_claim_tests` but doesn't follow the 20+ crate convention. Most visible gap: `fee-controller` (whitepaper centerpiece) and `decay-lamport` (Tier-1). |

**Primitive coverage table (full version in agent output):** Of the 5 Tier-0 primitives — Light-Cone Ledger, Evap-Fork Cert, Singh Attractor Consensus, Bell-Certified Beacon, Immune Validator Set — all 5 have a real crate, `tests/coverage.rs`, `tests/e2e.rs`, and source citations of `INVENTION_STACK.md §X.Y`. Doctrine adherence is very high: 396 `INVENTION_STACK.md` citations across the crate tree.

**No "claim has no implementation" found** at Tier-0/Tier-1/Tier-2 levels. Every doctrine primitive maps to at least a scaffolded crate.

**MERA gate (FAIL):** code respect is PARTIAL — no consumer reads it, but it's still computed per-block. Should be deleted.

**Causal-CHSH gate (PASS):** code respect FULL but doc claims stale. Crate lib + Cargo description need 1-line update.

---

## 5. Execution + state + EvaporScript (agent: execution)

| ID | Sev | Site | Class |
|---|---|---|---|
| STATE-2 | **CRITICAL** | `state/src/rocksdb_backend.rs:1351-1361, 1363-1384` | **9 RocksDB trait methods are no-op stubs**: `put_proposal`, `get_proposal`, `all_proposals`, `get_governance_param`, `put_governance_param`, `commit_state_snapshot`, `prune_snapshots_before`, `get_account_at_height`, `get_object_at_height`. `InMemoryStateDB` implements them correctly → tests pass; cluster runs RocksDB → cluster never persisted a proposal. **Note:** CLAUDE.md just flipped `conservation_enforcement` default to `"enforce"` (commit 76d95590) — if that default is applied at the executor level, conservation enforcement still works; but other params (`block_gas_limit`, `base_fee_floor`, etc.) are still dead. ✅ **CLOSED 2026-05-28 spot-check:** all 9 functions are now real implementations in `rocksdb_backend.rs` lines 1469–1535 (`get_proposal:1469`, `put_proposal:1473`, `all_proposals:1478`, `get_governance_param:1484`, `put_governance_param:1488`, `commit_state_snapshot:1497`, `get_account_at_height:1507`, `get_object_at_height:1517`, `prune_snapshots_before:1535`). |
| SBA-1 | **CRITICAL** | `contracts/evaporscript/sealed_bid_auction.es:68-93` | `commit(commit_hash)` ignores the hash arg (only `committed[caller]=1`); `reveal()` never recomputes or compares. **NX4 commit-reveal binding exists at the substrate / Rust layer but not in the reference contract.** ✅ **CLOSED 2026-05-28 spot-check:** `commit(commit_hash: string)` at line 82 now `committed_hashes[caller] = commit_hash`; `reveal(nominal_bid, effective_bid, commitment_hash)` at line 98 carries the hash for round-trip verification; new `committed_hash_of(who)` accessor at line 163. |
| NFT-1 | **CRITICAL (language design)** | `contracts/evaporscript/mortal_nft.es:33`; `parser.rs:367` | `state { owner }` shadow-collides with the EvaporScript builtin `owner` (deployer). Contract is correct as written; any future refactor dropping `self.` silently flips auth gate. |
| LOTTERY-1 | HIGH | `contracts/evaporscript/lottery.es:59-70` | Operator-supplied `proof: string` accepted as-is, never verified against on-chain VRF beacon. Should use `VrfDomainRandomness` / `RandomRange` opcodes. |
| SDDC-1 / SHLM-1 | HIGH | `contracts/evaporscript/sddc.es:143-161`, `shlm.es:166-191` | Open-callable settlement entry points (`try_clear`, `record_match`). Adversary races the legitimate coordinator. |
| SPLIT-1 | HIGH | `contracts/evaporscript/payment_split.es:67, 79, 85` | `total_deposited * bps / 10000` uses `checked_mul`; at `total_deposited * bps > u64::MAX` (boundary ~1.8e15) every claim reverts → contract bricks at scale. |
| EXEC-2 | HIGH | `execution/lib.rs:1553-1561, 1747-1755` | `call_depth += 1` fires before args-size + serde-parse error returns. Errors return without decrementing. After MAX_CALL_DEPTH bad txs, all subsequent CallContract / CallScript hard-error with `CallDepthExceeded` — **including legitimate ones**, across blocks. |
| OPCODE-1 | MED | `script/src/vm.rs:826-836` | `Op::VrfDomainRandomness` charges flat `GAS_STATE_LOAD=5` regardless of popped `domain` length (up to 1 MiB BLAKE3). M15 anti-pattern in new opcode. |
| OPCODE-5 | MED | `script/src/vm.rs:711-724, 726-764` | `Op::Emit` / `Op::EmitEvent` bypass `track_memory`. MAX_MEMORY_BYTES=4MB cap doesn't apply to events. 64 MAX_STRING_LEN emits ≈ 85 gas but ~64 MB enqueued. |
| SFSV-1 | MED | `contracts/evaporscript/future_self_vault.es:139-156` | `record_sale(winner_addr)` no caller restriction. Listing-state guard has a race window with `cancel_listing`. |
| VEST-1 | MED | `contracts/evaporscript/vesting_schedule.es:70, 85, 119, 133` | `total_grant * elapsed` `checked_mul`. Soft DoS at large grants. |
| RULE-1 | MED | `contracts/src/lib.rs:642` | `energy_cost += cost` raw `+=`. Multiple `CostEnergy(near-u64::MAX)` rules wrap silently. |
| PARSE-1 | MED | `script/src/parser.rs:853-886, 888+, 714` | `parse_if` / `parse_while` / `parse_block` recurse unboundedly; only `parse_expr` has `MAX_EXPR_DEPTH=64`. 65 KiB source of nested `if{}` blows validator thread stack. |
| BOUNTY-1 | MED | `contracts/evaporscript/bounty.es:112-114` | `submission_of(who)` returns `Value::U64(0)` on missing key when caller expects string → runtime type-mismatch. |
| OPCODE-2/3/4, EXEC-1, SUB-1, POOL-1, RULE-2, WAL-1 | LOW | various | gas asymmetries, micro-DoS, dead module hazards |

**CLEAN:** BlockSTM OCC correctness (current_version mismatch → re-exec; abort ceiling prevents O(N²) storm), conservation gate at block granularity (4-compartment snapshot), all 44 opcodes pre-charge (G1+G2 closed), parser caps `MAX_TOKENS=1M / MAX_EXPR_DEPTH=64 / MAX_STRING_LITERAL=65K`, constant-fold + DCE preserve semantics, C3 (cert-before-state) intact, M10 BatchUndoLog stakes+delegations+sentinel intact, M11 WAL crash-atomic intact, M12 snapshot apply atomic intact, K4 chain_id length-prefix + AUDIT-I1 address binding intact, `mortal_message.es` reference pilot clean.

**Tests on Mini 1:** 559 execution + 140 script + 253 state — all green.

---

## 6. RPC + node + paymaster + MCP (agent: rpc)

| ID | Sev | Site | Class |
|---|---|---|---|
| A1 | **CRITICAL** | `node/api.rs:12820` (`wallet_sign_tx`), `:12895` (`wallet_submit_tx`) feeding `:12787` (`try_sign_with_wallet_key`) | **Wallet impersonation.** Handler authenticates session, then calls `try_sign_with_wallet_key(state, from_full, ...)` — looks up keys by ADDRESS only, no `user_id` filter. Any authenticated user passes `req.from = <victim_address>`; helper decrypts victim's ML-DSA key with shared `EVAPORCHAIN_KEY_MASTER`, signs arbitrary tx, returns/submits. `blake3(pk) == victim_from` so AUDIT-I1 binding passes. **Drain any wallet from any account.** `post_transfer:10714` does it correctly — these handlers were added later and skipped `require_wallet_ownership`. |
| A2 | HIGH | `node/api.rs:7836` (`post_settle_demurrage`) | Verifies ML-DSA over `{type:"settle_demurrage", from, current_epoch}` but **never checks `blake3(pk) == from`**. Same AUDIT-I1 class, new endpoint. Mempool bypassed (direct DB mutation). |
| A3 | HIGH | `node/api.rs:14431-14710` (5 Singh pool handlers: `post_pool_{mint,withdraw,reanchor}` + 2 swaps) | `require_tx_auth` called but `require_wallet_ownership` on `req.holder` skipped. Any logged-in user can burn anyone's LP shares (`withdraw`), refresh someone else's energy clock (`reanchor`), or mint into their position. `post_token_transfer:14173` does it correctly. |
| A4 | MED | `node/api.rs:17843` (`hex_to_32`); call sites `:17716, :17789, :17878` | R1 drift — `hex_to_32` doesn't pre-cap length before `hex::decode` allocates. R1's `parse_hex32:3412` correctly pre-caps at 64 chars. Helper introduced post-R1 without same guard. Body limit caps worst case ~2MB but anti-pattern is back. |
| A5 | MED | `node/api.rs:19281` (`is_mcp_gated_path`) vs `mcp/src/tools.rs:541, 566, 594, 624, 653` | MCP gate allowlist drift. 3 tools (`compute_demurrage`, `check_annealing_temperature`, `check_conservation`) POST to paths NOT in allowlist. Pure-compute endpoints — impact low — but the gate's claim of "every MCP-tool path is gated" is false. |
| A6 | MED | `node/api.rs:18442` (`get_snapshot_download`) | No per-IP rate-limit. Global 200/IP/10s applies but single attacker sustains 20 req/s of multi-GB tokio-streamed reads forever. Faucet has dedicated bucket; snapshot doesn't. |
| A7 | LOW | `node/api.rs:15659` (`post_propose`), `:14063` (`post_deploy_token`) | `creator` / `deployer` fields aren't bound to session. In-memory testnet stores → cosmetic/reputational. |
| A8 | LOW | `node/jsonrpc.rs:287` (`rpc_send_raw_tx`) | Hashes `blake3(serde_json::to_vec(&tx))` instead of `blake3(tx.signable_bytes())`. JSON-RPC client gets a hash that NEVER matches chain-recorded hash → `/api/tx/<hash>` polling stuck forever. Breaks ethers.js / web3.py UX. |

**Pattern across A1+A2+A3:** newer handlers in `api.rs` skip the `require_wallet_ownership` check that older handlers correctly perform. Systemic checklist-gap, not one-off. Suggested CI lint: every POST endpoint taking `from`/`holder`/`creator` field MUST have `require_wallet_ownership` OR `blake3(pk) == addr` in the function body.

**Drift check:** every R1–R14 / E1–E6 / O-series / F1a / G3 / I1 / M11 / Network-1 / MCP-AUTH-001 close verified still at its cited line.

**CLEAN:** Paymaster runbook claims all hold (audit-log fsync, idempotency persist, per-sender rate, inner-variant whitelist, SIGHUP rotate, drift reconciliation). All 5 MCP write-tools carry `requiresConsent: true` + per-tool rate-limit + audit log. No secrets in `tracing::info!`/`debug!`. All `user_db` queries use prepared statements (no SQL injection). WS 4096 subscriber cap intact.

**Tests on Mini 1:** 252+83+18 — all green.

---

## 7. Formal proof + spec alignment (agent: proof)

**Build status:** Coq compiles green under Rocq 9.1.1 (`make` in `research/coq/`, exit 0). TLC unverified — no JRE on Mini cluster (`brew install temurin` needed).

**Zero `Admitted.`** in any `.v` file confirmed by grep — but several theorems are *axiomatised* rather than mechanically discharged:

| File | Load-bearing axioms | What the headline theorem actually proves |
|---|---|---|
| `EnergyDecayMonotonicity.v` | None | The full thing — Qed. |
| `EnergyVerkleCompression.v` | 1 (`compress_preserves_commitment` — Pedersen homomorphism, correct deferral) | Local invariants over each replacement site. |
| `PoHAFreeloading.v` | 9 (including `freeloading_reduction` — the actual cryptographic-game argument is *stated*, not proved) | Skeleton reduction. |
| `LazyEagerEquivalence.v` | 2 (ideal section); concrete uses `EnergyDecayMonotonicity` directly | Ideal `lazy=eager` plus concrete one-sided drift bound `lazy ≤ eager`. NOT a magnitude bound. |
| `LLSAInvariantPreservation.v` | None | Parametric conservation gate. BUT decay branch never exercised (every successor sets `epochs_elapsed=0`); proof is vacuous on `Inv` decay-floor side. |
| `EvaporChainSafetyLiveness.v` | None mechanically | A reachability-induction wrapper conditional on user-supplied safety + liveness preservation hypotheses. Headline overclaims. |

**Hand-waved / load-bearing steps in frontier proofs:**

| Sev | File / section | Finding |
|---|---|---|
| **HIGH** | `frontier/02-energy-verkle-trie-proof.md` §4 + `tla/EnergyVerkleTrie.tla::DecompressOnInsert` | Both formalise a `decompress` operation that does NOT exist in `energy_verkle.rs`. Frontier #2 cites a fictitious snippet at `energy_verkle.rs:352-355`. Coq companion correctly says no decompress exists. **TLA model + markdown proof are out of sync with the actual chain.** |
| MED | `frontier/03-rule-based-consensus-proof.md` §4.1 | Claims "relative error below 0.1%" for lazy↔eager drift. `LazyEagerEquivalence.v` only proves one-sided `lazy ≤ eager`, and its own comments contradict the magnitude claim. |
| MED | `EvaporChainBFT.tla::ReceiveProposalAndPrevote` | TLA permits vote when `lockedRound < r` even if `lockedBlock ≠ block`. Rust `tendermint.rs:5479-5490` is **stricter** (votes Nil whenever locked hash differs). TLA models a weaker safety property than impl enforces. |
| MED | `ConservationInvariant.tla::DecayFloor` | Uses pure bit-shift halving; Rust uses full `energy_at_epoch` with linear interpolation. Rust floor is tighter. |
| MED | `PoHAFreeloading.v` | Headline doctrine claim "Coq-proven" should be qualified — game-theoretic argument is axiomatised, not proved. Standard practice for crypto-game theorems, but doctrine language doesn't qualify it. |
| LOW | `LazyEagerEquivalence.v:232, 348, 709, 753-774` | Stale "Left as Admitted" comments — corresponding lemmas are now Qed. Mislead readers. |
| LOW | `frontier/01-poha-decaying-da-proof.md` + `frontier/03-rule-based-consensus-proof.md` | Source line drift: `is_supermajority` at `poha.rs:149` not `:131`; `energy_at_epoch` at `types/lib.rs:2064` not `state/evaporation.rs`. |

**Trace files** (`EvaporChainBFT_TTrace_*.tla`) are real counter-examples matching the current spec — not orphans.

---

## Cross-cutting themes

1. **Doctrine drift is the dominant finding pattern.** MERA on hot path despite FAIL verdict (#1). Causal-CHSH still flagged GATED despite double PASS (#4). Light-Cone "Soul of the chain" framing not yet shipped (#4). `decompress` formalised in TLA + frontier proof but missing in Rust (#7). `commitments` field in VerkleProof dead but visible (#1). 6 cases of "the artifact and the implementation disagree on what the chain does."

2. **Crypto regressions in the H2 closure.** `EnergyNode::hash` got the half-fix — verify side added DSTs but producer side didn't. Test is red on HEAD. CR-1, CR-2, CR-3 are the same crate and overlap in fix shape.

3. **DA-cert is a forgery surface.** Q1+Q2+Q3 stack to single-key forgery with one valid BLS sig over an attacker-chosen message. Fix all three in one commit (`stake` into signed message + `total_stake` cross-check + dedup) and the chain class is closed.

4. **Newer endpoints skip the wallet-ownership check.** A1+A2+A3 are 7 endpoints in `node/api.rs` added after the AUDIT-I1 / wallet-ownership pattern was established. Older handlers (`post_transfer`, `post_token_transfer`) do it right; newer ones don't. CI lint or shared `require_signed_for(addr)` helper needed.

5. **RocksDB stub coverage**. STATE-2 + CONS-A combine to leave production validators without governance plumbing for execution-layer params. With `conservation_enforcement` now defaulting to `"enforce"` (CLAUDE.md update), conservation is OK out of the box, but `block_gas_limit` / `base_fee_floor` / `crooks_mev_settlement_mode` / `light_cone_state_branches_enabled` / `lambda_fold_mode` remain on startup defaults forever on RocksDB.

6. **Formal proof headlines overclaim.** Decay-BFT safety+liveness is a reachability wrapper. PoHA freeloading is axiomatised. LLSA invariant is vacuous on the decay-floor side. The proofs are honest; the doctrine summary isn't.

7. **Parser + executor have soft-DoS hazards.** PARSE-1 (unbounded stmt-block recursion) and EXEC-2 (call_depth leak across blocks) are cheap-to-trigger validator-CPU paths.

---

## Recommended drive order (one-line summaries)

| Order | Finding | Why first |
|---|---|---|
| 1 | CR-1 | HEAD is red. Nothing else ships until green. |
| 2 | CR-2, CR-3 | Same crate, same DST class, same patch shape. |
| 3 | Q1 + Q2 + Q3 (one commit) | Closes single-key DA-cert forgery. |
| 4 | A1 + A2 + A3 (one shared helper) | Closes wallet-impersonation surface. |
| 5 | STATE-2 | Production governance plumbing dead on RocksDB. |
| 6 | SBA-1 + LOTTERY-1 + SDDC-1 + SHLM-1 + SPLIT-1 + SFSV-1 | Reference contracts need binding + auth + arithmetic fixes before they're consumed by launch dApps. |
| 7 | Q4 (strict `>` everywhere) | Quorum-class bug; sweep workspace. |
| 8 | Q5 (stake-weighted antichain) | H11 regression for the antichain path. |
| 9 | Q6, Q8 (canonical seed; verify_signatures_with_active) | DA path hardening. |
| 10 | Q7 (bridge state-proof Merkle) | Tree-size confusion class. |
| 11 | EXEC-2, PARSE-1 | Cheap DoS. |
| 12 | L0-A, CONS-A | Doctrine: lambda-fold half-life + governance read path. |
| 13 | INV-HIGH-1, INV-HIGH-2 (one commit: delete `mera_integration.rs` call sites + fix Cargo.toml) | Doctrine truth. |
| 14 | INV-MED-3 (Causal-CHSH stale GATED tag) | 1-line clarity fix. |
| 15 | Frontier #2 decompress drift + Frontier #3 magnitude claim | Doctrine truth in proofs. |
| 16+ | All remaining MEDIUMs and LOWs | Targeted commits per crate. |

---

## File inventory

This document at `AUDIT_2026_05_17.md`. Raw agent outputs at:
- `/private/tmp/claude-501/-Users-satyawansingh/e3eadc43-216e-426e-b0c0-7d0c57fac168/tasks/a524f74ce19469233.output` — crypto
- `aba4886cd05f72b86.output` — consensus + DA
- `a04e00b626496e237.output` — energy + conservation
- `a7627b785b05f3c44.output` — invention-stack
- `aa3844510100bc4f2.output` — execution + EvaporScript
- `ab1b189e0df3fd13e.output` — RPC + paymaster + MCP
- `a77cea800edd77526.output` — formal proofs + TLA
