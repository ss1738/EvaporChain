[package]
name = "evaporchain-hlwa"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Half-Life Wrapped Asset (HLWA). Wrapped tokens decay unless re-attested by origin chain — eliminates infinite-bridge-supply hacks (Ronin / Wormhole / Nomad). Per INVENTION_STACK.md §4.2 (Tier 2)."

[dependencies]
evaporchain-energy-kernel = { path = "../evaporchain-energy-kernel" }
evaporchain-types = { path = "../evaporchain-types" }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = "1"
