[package]
name = "evaporchain-hlts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Singh-Shamir Cells (HLTS). Half-life threshold shares — secret recoverable only by surviving high-energy quorum. Per INVENTION_STACK.md §4.2 (Tier 2)."

[dependencies]
evaporchain-energy-kernel = { path = "../evaporchain-energy-kernel" }
evaporchain-types = { path = "../evaporchain-types" }
blake3 = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = "1"
