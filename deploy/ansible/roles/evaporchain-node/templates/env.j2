# Environment file for evaporchain-node.service.
# Rendered by Ansible — secrets come from group_vars/vault.yml.
# Mode 0600, owned by {{ evaporchain_user }}.
{% for key, value in node_env.items() %}
{{ key }}={{ value }}
{% endfor %}
